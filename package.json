{
  "name": "next",
  "version": "1.0.0",
  "main": "index.js",
  "author": "Shivam Mishra <mishivam1999@gmail.com>",
  "license": "MIT",
  "private": true,
  "workspaces":[
    "packages/*"
  ],
  "scripts": {
    "dev": "next dev",
    "build": "yarn workspaces foreach run next build",
    "start": "yarn workspaces packages/web start next start -p $PORT",
    "heroku-postbuild": "yarn start",
  "test": "echo \"Error: no test specified\" && exit 1"
  }
}
