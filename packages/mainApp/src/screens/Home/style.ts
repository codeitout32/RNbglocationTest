import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  header: {
    position: 'absolute',
    zIndex: 100,
    height: 100,
  },
  view: {
    flex: 1,
    position: 'relative',
  },
});

export default styles;
